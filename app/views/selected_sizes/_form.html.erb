<%= form_for(@selected_size) do |f| %>
  <% if @selected_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@selected_size.errors.count, "error") %> prohibited this selected_size from being saved:</h2>

      <ul>
      <% @selected_size.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cart_item_id %><br>
    <%= f.number_field :cart_item_id %>
  </div>
  <div class="field">
    <%= f.label :item_size_id %><br>
    <%= f.number_field :item_size_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
